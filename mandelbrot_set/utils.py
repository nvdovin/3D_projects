# Configurations

WIGHT = HEIGHT = 600
# HEIGHT = round((9 / 16) * WIGHT)
h = WIGHT / HEIGHT

x_center = (WIGHT // 2) + (WIGHT // 5)
y_center = HEIGHT // 2

# Colors

BLACK = (10, 7, 12)
WHITE = (255, 255, 255)
